{
  "name": "@kamilmielnik/trie",
  "version": "0.0.2-1",
  "description": "Trie data structure implementation (in TypeScript)",
  "license": "MIT",
  "author": {
    "name": "Kamil Mielnik",
    "email": "kamil.adam.mielnik@gmail.com",
    "url": "https://kamilmielnik.com/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kamilmielnik/re-trie.git"
  },
  "bugs": {
    "url": "https://github.com/kamilmielnik/re-trie/issues"
  },
  "homepage": "https://github.com/kamilmielnik/re-trie#readme",
  "main": "./dist/index.js",
  "files": [
    "dist/**"
  ],
  "types": "./dist/index.d.ts",
  "np": {
    "yarn": false
  },
  "publishConfig": {
    "access": "public"
  },
  "jest": {
    "collectCoverage": true,
    "collectCoverageFrom": [
      "src/**/*.{js,ts}"
    ],
    "coveragePathIgnorePatterns": [
      "node_modules/",
      "src/index.ts"
    ],
    "globals": {
      "ts-jest": {
        "tsconfig": "tsconfig.json"
      }
    },
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc --project .",
    "prettier": "prettier --list-different './src/**/*.{js,ts}'",
    "prettier:fix": "npm run prettier:js -- --write --no-list-different",
    "prebuild": "npm run clean",
    "prerelease": "npm run build",
    "release": "np --no-2fa",
    "test": "jest --no-cache",
    "test:watch": "jest --no-cache --watch"
  },
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "jest": "^26.6.3",
    "np": "^7.0.0",
    "prettier": "^2.1.2",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.4.3",
    "typescript": "^4.0.5"
  }
}
