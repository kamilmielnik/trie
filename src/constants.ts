/**
 * Represents end of a node in JSON (serialized format).
 */
export const CLOSE_PARENS = ')';

/**
 * Represents start of a node in JSON (serialized format).
 */
export const OPEN_PARENS = '(';

/**
 * Separates nodes in JSON (serialized format).
 */
export const SEPARATOR = ',';
